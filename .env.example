# Optional AI configuration only. Classification works without an API key.
# Copy to the ignored .env file and replace the placeholder locally.
OPENAI_API_KEY=your_api_key_here
# Preserve the existing model or set one available to the configured account.
OPENAI_MODEL=gpt-5.6-luna
